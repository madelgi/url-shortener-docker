version: "3.7"
services:
    web:
        env_file:
            - ./.env.dev
        volumes: [
            './web:/home/url_shortener/web',
        ]

volumes:
    url-shortener-db:
        name: url-shortener-db
